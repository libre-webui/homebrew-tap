# homebrew-tap

Homebrew tap for Libre WebUI.

## Installation

```bash
# Add the tap
brew tap libre-webui/tap

# Install CLI version
brew install libre-webui

# Or install desktop app
brew install --cask libre-webui
```
